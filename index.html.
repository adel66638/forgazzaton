<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Forgazzaton.io | فورتون غزة</title>
    <script src="https://unpkg.com/@tonconnect/ui@latest/dist/tonconnect-ui.min.js"></script>
    <style>
        :root {
            --primary: #1aed1a; /* لون أخضر (رمز النمو وغزة) */
            --gold: #ffd700;    /* لون ذهبي (رمز الأرباح) */
            --dark: #0a0a0a;
        }
        body {
            background-color: var(--dark);
            color: #fff;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            margin: 0;
            display: flex;
            align-items: center;
            justify-content: center;
            min-height: 100vh;
            overflow-x: hidden;
        }
        .container {
            text-align: center;
            padding: 20px;
            max-width: 500px;
            width: 90%;
            border: 1px solid rgba(26, 237, 26, 0.3);
            border-radius: 25px;
            background: rgba(255, 255, 255, 0.02);
            backdrop-filter: blur(15px);
            box-shadow: 0 10px 30px rgba(0,0,0,0.5);
        }
        .logo {
            font-size: 60px;
            margin-bottom: 10px;
            filter: drop-shadow(0 0 10px var(--primary));
        }
        h1 {
            color: var(--gold);
            letter-spacing: 2px;
            margin: 10px 0;
            font-size: 28px;
        }
        p {
            color: #ccc;
            line-height: 1.6;
            font-size: 15px;
        }
        .features {
            display: flex;
            justify-content: space-around;
            margin: 25px 0;
            font-size: 13px;
        }
        .feature-item {
            color: var(--primary);
            border: 1px solid var(--primary);
            padding: 5px 10px;
            border-radius: 10px;
        }
        #ton-connect-button {
            display: flex;
            justify-content: center;
            margin: 30px 0;
        }
        .footer {
            font-size: 12px;
            color: #666;
            margin-top: 20px;
        }
        /* زر التفعيل بعد الربط */
        #activate-section {
            display: none;
            margin-top: 20px;
            padding-top: 20px;
            border-top: 1px dashed #444;
        }
        .btn-activate {
            background: linear-gradient(45deg, var(--primary), #0b9e0b);
            color: black;
            border: none;
            padding: 15px 40px;
            border-radius: 12px;
            font-weight: bold;
            cursor: pointer;
            width: 100%;
            font-size: 16px;
        }
    </style>
</head>
<body>

    <div class="container">
        <div class="logo">🟢</div>
        <h1>FORGAZZATON.IO</h1>
        <p>ابدأ بـ 1$ فقط. حقق أرباحاً مذهلة وساهم في إغاثة غزة عبر أقوى عقد ذكي على شبكة TON.</p>

        <div class="features">
            <span class="feature-item">50% عمولة</span>
            <span class="feature-item">10% لغزة</span>
            <span class="feature-item">16 مستوى</span>
        </div>

        <div id="ton-connect-button"></div>

        <div id="activate-section">
            <p style="color: var(--gold);">المحفظة متصلة جاهز للانطلاق!</p>
            <button class="btn-activate" onclick="sendTransaction()">تفعيل الحساب (1 TON)</button>
        </div>

        <div class="footer">
            لا مركزية كاملة | لا يوجد وسيط | أرباح فورية
        </div>
    </div>

    <script>
        // إعدادات TON Connect
        const tonConnectUI = new TONConnectUI.TonConnectUI({
            manifestUrl: 'https://raw.githubusercontent.com/ton-community/tutorials/main/03-client/test/public/tonconnect-manifest.json', // مؤقت
            buttonRootId: 'ton-connect-button'
        });

        // مراقبة حالة المحفظة
        tonConnectUI.onStatusChange(wallet => {
            if (wallet) {
                document.getElementById('activate-section').style.display = 'block';
            } else {
                document.getElementById('activate-section').style.display = 'none';
            }
        });

        // وظيفة إرسال الـ 1 دولار (الـ TON)
        async function sendTransaction() {
            const transaction = {
                validUntil: Math.floor(Date.now() / 1000) + 60, // دقيقة واحدة للصلاحية
                messages: [
                    {
                        address: "UQCXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX", // اتركها هكذا حتى تبرمج العقد وتضع عنوانه هنا
                        amount: "1000000000", // 1 TON بالنانو
                    }
                ]
            };

            try {
                const result = await tonConnectUI.sendTransaction(transaction);
                alert("تم إرسال طلب التفعيل! تفقد محفظتك.");
            } catch (e) {
                console.error(e);
                alert("حدث خطأ أو تم إلغاء العملية.");
            }
        }
    </script>
</body>
</html>
